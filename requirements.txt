# Pinned for Python 3.14 on Windows (win_amd64 / cp314).
#
# IMPORTANT -- torch and torchvision are NOT installed from PyPI here.
# PyPI serves CPU-only builds. For GPU training you must install the +cu126
# variants from PyTorch's own index FIRST, then install the rest:
#
#   python -m pip install torch==2.13.0+cu126 torchvision==0.28.0+cu126 `
#       --index-url https://download.pytorch.org/whl/cu126
#   python -m pip install -r requirements.txt
#
# Wheel availability on Python 3.14 + Windows is narrow and version-specific.
# Verified on 2026-08-01 against download.pytorch.org:
#
#   index    torch     torchvision   cp314 win_amd64
#   cu126    2.13.0    0.28.0        yes   <- used here
#   cu128    2.11.0    0.26.0        yes
#   cu129    2.9.0     (none)        NO    <- torchvision missing, do not use
#   cu130    2.13.0    0.28.0        yes
#
# Installing plain `torch` from PyPI silently gives a CPU build, and medmnist
# will pull one in as a dependency if torch is absent. Always verify:
#   python -c "import torch; print(torch.cuda.is_available())"

torch==2.13.0
torchvision==0.28.0

medmnist==3.0.2
scikit-learn==1.9.0
numpy==2.5.1
scipy==1.18.0
pandas==3.0.5
matplotlib==3.11.1
seaborn==0.13.2
Pillow==12.3.0
tqdm==4.70.0
